using UnityEditor;
using TMPro;

public class MessageTMPInputField : TMP_InputField
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
